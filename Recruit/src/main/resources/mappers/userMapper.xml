<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.UserMapper">

<!-- 	<select id="login" resultType="UserVO">

		select uid, upw, uname from
		tbl_user where uid = #{uid} and upw = #{upw}

	</select> -->
	
	<!--uname은 뺐음  -->
	<!--uid는 id, upw는 pw, tbl_user는 tbl_board  -->
	<select id="login" resultType="UserVO">

		select id, pw from
		tbl_board where id = #{id} and pw = #{pw}

	</select>
	

<!-- 	<update id="keepLogin">
		update tbl_user set sessionKey = #{sessionId},
		sessionLimit = #{next} where
		uid = #{uid}
	</update> -->
	
	<!--uid는 id, tbl_user는 tbl_board  -->
	<update id="keepLogin">
		update tbl_board set sessionKey = #{sessionId},
		sessionLimit = #{next} where
		id = #{id}
	</update>
	

	<!-- <select id="checkUserWithSessionKey" resultType="UserVO">
		select * from
		tbl_user where sessionKey = #{value} and sessionlimit > now()
	</select> -->
	
	<!--tbl_user는 tbl_board  -->
	<select id="checkUserWithSessionKey" resultType="UserVO">
		select * from
		tbl_board where sessionKey = #{value} and sessionlimit > now()
	</select>

</mapper>


